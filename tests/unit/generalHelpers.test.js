import { afterEach, describe, expect, it } from "vitest";
import { cleanup } from "@testing-library/svelte";

describe('Testing general helper functions', () => {
    afterEach(() => {cleanup});
    // currently nothing needs testing here

    it('there is nothing to test yet', () => {
        console.log('Nothing to test');
    })
})